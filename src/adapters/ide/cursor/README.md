# Cursor adapter

O Cursor é o host. Ele não substitui o JARVIS.

1. Instale o CLI global: `npm install -g .` na pasta do runtime.
2. A rule e o skill pessoais em `~/.cursor` mandam o agente chamar `jarvis` no workspace aberto.
3. Em qualquer projeto: "jarvis planeje X" ou `/jarvis:plan X`.
